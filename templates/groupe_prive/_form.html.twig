{% form_theme form 'bootstrap_5_horizontal_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}

    {{ include('inc/liste_users.html.twig') }}

    <button type="submit" class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>

    {% if app.request.get('_route') != "participant_list"%}
        <a href="{{ path('groupe_prive_list') }}" class="btn btn-info">Retour</a>
    {% endif %}

{{ form_end(form) }}
