{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Éditer une Ville {% endblock %}

{% block body %}

    <div class="w-50 mx-auto d-flex flex-column align-items-center">

        <h1>Modification d'une ville</h1>

            {{ form_start(form) }}

            {{ form_widget(form) }}

            {{ include('inc/buttons.html.twig', {'isForm': true, 'update': true, 'bigButton': true}) }}

            {{ include('inc/buttons.html.twig', {'return': true, 'bigButton': true, 'type': 'ville'}) }}

            {{ form_end(form) }}

    </div>

{% endblock %}
