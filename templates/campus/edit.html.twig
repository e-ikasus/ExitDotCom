{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Éditer un Campus {% endblock %}

{% block body %}

    <div class="d-flex flex-column align-items-center">

        <h1>Modification d'un campus</h1>

        <div class="w-50">
            {% form_theme form 'bootstrap_5_horizontal_layout.html.twig' %}

            {{ form_start(form) }}

            {{ form_widget(form) }}
        </div>

        <div class="d-flex gap-2">
            {{ include('inc/buttons.html.twig', {'isForm': true, 'update': true, 'bigButton': true}) }}

            <a class="btn btn-success" onclick="history.go(-1);">Retour</a>
        </div>


        {{ form_end(form) }}

    </div>

{% endblock %}
