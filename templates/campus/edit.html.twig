{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Éditer Campus {% endblock %}

{% block body %}
    <h1>Edit Campus</h1>

    {{ include('campus/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('campus_list') }}">back to list</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
