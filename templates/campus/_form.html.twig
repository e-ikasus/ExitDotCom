{% form_theme form 'bootstrap_5_horizontal_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-success">{{ button_label|default('Ajouter Campus') }}</button>
{{ form_end(form) }}
