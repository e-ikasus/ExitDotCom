{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Éditer Lieu {% endblock %}

{% block body %}
    <h1>Edit Lieu</h1>

    {{ include('lieu/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lieu_list') }}">back to list</a>

    {{ include('lieu/_delete_form.html.twig') }}
{% endblock %}
