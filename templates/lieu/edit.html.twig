{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Modifier un lieu {% endblock %}

{% block body %}
    <h1>Modifier le lieu "{{ lieu.nom }}"</h1>

    {{ include('lieu/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('lieu/_delete_form.html.twig') }}
{% endblock %}
