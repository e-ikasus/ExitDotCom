{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Nouveau Participant{% endblock %}

{% block body %}
    <h1>Create new Participant</h1>

    {{form_start(form)}}

    {{form_widget(form)}}

    <button class="btn btn-lg btn-success">Importer CSV</button>

    {{form_end(form)}}


    <a href="{{ path('participant_list') }}">back to list</a>
{% endblock %}
