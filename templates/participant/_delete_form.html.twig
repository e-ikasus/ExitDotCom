<form method="post" action="{{ path('participant_delete', {'pseudo': participant.pseudo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
