<div class="d-flex justify-content-between">

    <div class="monLogo">
        <img src="{{ asset('image/EXIT LOGO.png') }}" alt="Logo du site">
    </div>

    <div class="maNavBar align-self-center">
        <nav class="navbar navbar-expand-md navbar-dark bg-primary ms-5">
            <div class="container-fluid">
{#                <a class="navbar-brand" href="{{ path('sortie_list') }}" title="Retour a l'accueil">ExitDotCom</a>#}
                <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav ms-auto">

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('sortie_list') }}">Accueil</a>
                        </li>

                        {% if is_granted ('ROLE_ADMIN') %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
                                   aria-haspopup="true" aria-expanded="false">Admin</a>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="{{ path('ville_list') }}">Gestion des Villes</a>
                                    <a class="dropdown-item" href="{{ path('campus_list') }}">Gestion des Campus</a>
                                    <a class="dropdown-item" href="{{ path('participant_list') }}">Gestion des Participants</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="{{ path('sortie_new') }}">Nouvelle Sortie</a>
                                    <a class="dropdown-item" href="{{ path('lieu_list') }}">Gestion des lieux</a>
                                    <a class="dropdown-item" href="#">Separated link</a>
                                </div>
                            </li>
                        {% endif %}

                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link"
                                   href="{{ path('participant_profil', {'pseudo': app.user.pseudo } ) }}">Mon
                                    profil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('logout') }}">Se déconnecter</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('login') }}">Se connecter</a>
                            </li>
                        {% endif %}
                    </ul>
                    {#            <form class="d-flex"> #}
                    {#                <input class="form-control me-sm-2" type="text" placeholder="Search"> #}
                    {#                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button> #}
                    {#            </form> #}
                </div>
            </div>
        </nav>
    </div>

</div>
