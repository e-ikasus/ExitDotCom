{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Éditer Sortie {% endblock %}

{% block body %}

    <h1>Éditer une Sortie</h1>

    {{ include('sortie/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('sortie_list') }}">back to list</a>

    {{ include('sortie/_delete_form.html.twig') }}
{% endblock %}
