{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | {{ app.request.get('_route') == "sortie_new" ? "Organiser une sortie" : "Modifier une sortie" }} {% endblock %}

{% block body %}

    <h1>{{ app.request.get('_route') == "sortie_new" ? "Organiser une sortie" : "Modifier une sortie" }}</h1>

    {{ include('sortie/_form.html.twig') }}

    <a href="{{ path('sortie_list') }}">
        <button class="btn btn-warning">Retour</button>
    </a>

    {% if app.request.get('_route') == "sortie_new" %}
    <script src="{{ asset('JS/KeepSortieFieldWhileAddingPlace.js') }}"></script>
    {% endif %}

{% endblock %}